[package]
name = "hello"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "hello"
path = "hello.rs"
crate-type = ["staticlib"]
test = false
bench = false

[dependencies]
rko-core = { path = "../../rko-core" }
