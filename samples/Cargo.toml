[workspace]
resolver = "2"

members = ["hello", "kvec_test", "rofs_test", "tarfs", "ext2", "workqueue_test", "tcp_echo", "async_echo", "kunit_tests"]

[workspace.dependencies]
rko-core = { path = "../rko-core" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
